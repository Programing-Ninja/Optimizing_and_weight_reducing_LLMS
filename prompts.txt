=== PROMPT LOG (CLAUDE.md rule 2) ===

[2026-06-15] (env setup, prior turn)
"did you make the env and make permissions as it was supposed to have"
-> Found .env.example template + gitignore protection; no real .env existed.

[2026-06-15] (env setup, prior turn)
"create the .env and chmod 600 it"
-> Created /scratch/DA24B039/sct_tq/.env from template, chmod 600.

[2026-06-15] CURRENT PROMPT
"Now the task is to combine SCT and TQ at the same time for different energies
and essentially get the pareto frontier for when the compression, ram usage,
latencies and accuracy are the best. Create an experiment file for that, also
verify if the environment is conducive to run the experiment, else make a new
virtual environment with all the requirements. also dont forget to follow the
rules of CLAUDE.md and switch to a lighter model once you have mapped out what
to do for the experiment."
-> Plan written to path.txt. Building fresh venv (existing SCT/env torch is broken:
   CXXABI_1.3.15 missing; GPU driver mismatch -> CPU-only). Experiment:
   experiments/sct_tq_pareto.py sweeping SCT energy x TQ key/value bits, measuring
   compression / RAM / latency / perplexity -> Pareto frontier.

[2026-06-15] (implementation prompt)
"You are implementing a pre-designed experiment in /scratch/DA24B039/sct_tq. The PLAN is already written in /scratch/DA24B039/sct_tq/path.txt — read it first. Do NOT redesign; implement exactly. [...]"
-> Tasks 1-4 completed:
   TASK 1: Built /scratch/DA24B039/sct_tq/.venv from /opt/miniconda3/bin/python (3.13),
           installed torch 2.12 CPU, transformers 5.12, datasets, numpy, matplotlib,
           psutil, accelerate, spectral_compact_training, turboquant (editable).
           No libstdc++ fix needed (py3.13 links newer runtime). run.sh created precautionarily.
   TASK 2: Wrote experiments/sct_tq_pareto.py — full sweep with TurboQuantLayer
           (DynamicLayer subclass) for KV compression, SCT energy sweep, Pareto frontier.
   TASK 3: Smoke test passed: --quick --max-tokens 128 produced JSON results.
           ppl dense=22.76, dense+TQ(k=3,v=4)=81.82, weight=538MB, TQ-KV=783KB.
   TASK 4: Created iteration.txt, updated README.md with Experiments section.

[2026-06-15] PROMPT
"lets do both of these systematically, also we would need to tune the hyper
parameters wouldnt we? to get the best out of SCT, dont forget to follow the rules"
-> Plan in path.txt. Phase 1: HP-tune SCT on 135M (cheap) centered on the repo's
   known lever = PER-COMPONENT LR (uniform LR -> ppl 65-87; per-component +
   unfreeze attn -> target <10, per sct_per_component_lr.ipynb). Phase 2: apply
   best recipe to SmolLM2-1.7B (hidden=2048, where SCT actually compresses) +
   joint TQ Pareto. CPU-only (GPU dead); 31GB RAM ok for 1.7B; 1.7B finetune
   speed on CPU is the binding constraint -> measure ETA before committing.

=== [2026-06-15] PROMPT: Implement SCT per-component LR finetune + HP-tuning harness ===
Tasks A-E: (A) finetune_sct() in sct_finetune.py, (B) sct_hp_tune.py grid harness,
(C) wire real finetune into sct_tq_pareto.py, (D) --probe timing mode,
(E) smoke validate on 135M. Confirmed 44x ppl improvement (6318->144) in 120 steps.

[2026-06-15] upload a new screenshot for the hp_tune result to github and make a summary of the observations in the appropriate place

=== [2026-06-26] PROMPT: Scale up to Llama-3.1-8B + utility metric + recovery-LoRA pipeline ===
"Read the project and then make the following changes: First use LLama3.1 8B model
otherwise SCT's advantage is hidden. Next make a utility metric integrating
Perplexity, HellaSwag, MMLU and TruthfulQA, next implement utility recovery via
recovery-LoRA. Make it into a pipeline that I can run on an Nvidia A100, to analyse
the impact of different energy-weight compression on extent of quantization and the
converse, impact of quantization on the limit of compression via making a pareto
frontier graph of utility vs compression etc. First make a plan, ask whatever is
required, including making a secrets file, then start making the script + run
instructions."
-> Planning turn. Confirmed via questions: A100 40GB, BASE Llama-3.1-8B, SUBSET eval,
   recovery-LoRA TOGGLE (before/after). Plan written to path.txt (v3). Created .env
   secrets scaffold (HF_TOKEN for gated model). User to import SCT/TurboQuant/
   recovery-LoRA source; pipeline will bind to their real APIs before implementation.
-> User imported SCT/ + turboquant/ (recovery-LoRA = peft, implemented here). Built
   pipeline/ package (sct_apply, tq_cache, eval_tasks, utility, compression, recovery,
   pareto) + run_pareto.py orchestrator + run.sh + requirements.txt + README/iteration.

[2026-06-26] PROMPT
"add the --dry-run smoke test and then commit and push. also make a list of commands
to run when i import this on the hpc that i should run. remember to have a clean log
file which i can use to debug issues if any arises on the hpc. remember we will have
to make a new environment on the hpc since its the first time we are gonna be using it"
-> Added run_pareto.py --dry-run (model+SCT+TQ-cache forward+KV accounting+peft check,
   no datasets/training). run.sh now tees every run to logs/latest.log (+timestamped);
   added `dry` mode. Wrote HPC_SETUP.md (fresh venv on A100, ordered command list,
   SCRATCH HF cache, SLURM sbatch example). logs/ gitignored. Committed + pushed.

[2026-06-27] PROMPT
"In a previous conversation I asked you to go through this directory and our objective
of compression in terms of RAM usage of LLMs using weight compression (SCT),
quantization (TurboQuant) and structured pruning with minimal loss of utility
(incl. post-training recovery-LoRA), with utility metrics MMLU/HellaSwag/TruthfulQA.
Since I am at home without HPC access, you suggested theoretical analysis: model SCT
and TurboQuant, train a 1-2 layer linear-attention + linear-MLP model on Gaussian
data, analyse behaviour across SCT energies to validate the utility model, then the
combined SCT+TurboQuant effect theoretically. Objective: analyse the combined relation
of quantization and weight compression and find the optimal extent of both. Make a doc
with the suggestion + references, then continue."
-> Created theory/THEORETICAL_ANALYSIS.md: analytic error models (SCT = spectral
   low-rank BIAS ~ (1-eta) via Eckart-Young; TurboQuant = unbiased quantization
   VARIANCE ~ 2^(-2b)), combined rate-distortion objective dL ~ a(1-eta)+b*2^(-2b)
   with Lagrangian "equal marginal loss per byte" optimum, the toy validation
   experiment (Part B), step-by-step roadmap (Part C), grouped references (Part D),
   and open questions (Part E). No code yet; next action = scaffold theory/toy/ +
   theory/models/.
